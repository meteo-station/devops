---
- name: Initial server setup as root
  hosts: "raspberry"
  become: yes
  vars:
    ansible_user: root
    ansible_port: 22
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519_bonavi

  roles:
    - role: users
