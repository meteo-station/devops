---
- name: Initial server setup as root
  hosts: raspberry
  become: yes
  vars_files:
    - ../../secrets.yml
    - ../../env.yml
    - env.yml
    - secrets.yml
  vars:
    ansible_user: bonavi
    ansible_port: 2002
    ansible_ssh_private_key_file: ~/.ssh/id_rsa

  roles:
    - role: users
